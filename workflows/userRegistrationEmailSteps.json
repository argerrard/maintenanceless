{
  "Comment": "A State Machine to run the flow of sending newly registered users their verification and confirmation e-mails.",
  "StartAt": "SendUserVerificationEmail",
  "States": {
    "SendUserVerificationEmail": {
      "Comment": "This stage is responsible for sending the e-mail to the newly registered user containing the confirmation code.",
      "Type": "Task",
      "Resource": "arn:aws:states:::lambda:invoke.waitForTaskToken",
      "Parameters": {
        "FunctionName": "sendRegistrationVerification",
        "Payload": {
          "token.$": "$$.Task.Token",
          "email.$": "$.email",
          "confirmationCode.$": "$.confirmationCode"
        }
      },
      "Catch": [
        {
          "ErrorEquals": ["States.ALL"],
          "Next": "EmailVerificationFailure"
        }
      ],
      "TimeoutSeconds": 86400,
      "Next": "EmailVerificationSuccess"
    },
    "EmailVerificationSuccess": {
      "Type": "Pass",
      "Next": "SendUserConfirmationEmail"
    },
    "EmailVerificationFailure": {
      "Type": "Pass",
      "Result": "Failure",
      "End": true
    },
    "SendUserConfirmationEmail": {
      "Comment": "This stage is responsible for sending the e-mail to the newly confirmed user letting them know they are registered.",
      "Type": "Task",
      "Resource": "arn:aws:lambda:us-east-2:691442066446:function:sendConfirmationEmail",
      "Next": "UserIsRegistered"
    },
    "UserIsRegistered": {
      "Type": "Pass",
      "Result": "Success",
      "End": true
    }
  }
}
